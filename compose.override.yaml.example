# Optional local Compose overrides (copy to compose.override.yaml — gitignored pattern via local use).
# Example: expose Adminer only on loopback for development.
#
# services:
#   adminer:
#     image: adminer:4
#     ports:
#       - "127.0.0.1:8080:8080"
#     depends_on:
#       postgres:
#         condition: service_healthy
