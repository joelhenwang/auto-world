# Backend README — package layout
#
# Application code lives in `src/fictional_world/` (see handbook `19` §2–§3).
# Alembic config and migrations are added by S0-DB-001 / S0-DB-002.
# Do not add domain or ORM code in S0-ENG-001.

Package: `fictional_world`
Python: 3.12 (uv-managed from repository root)
