Your previous output did not satisfy the required JSON Schema.

<VALIDATION_ERRORS authority="trusted">
{{ sanitized_errors }}
</VALIDATION_ERRORS>

Return one complete replacement JSON object matching the original schema. Output JSON only, with no Markdown or discussion. Preserve valid proposed intent where possible, but obey all original authority, perspective, allowed-ID, Stage 1 action-family, and proposal-only rules. Do not add fields outside the schema.
