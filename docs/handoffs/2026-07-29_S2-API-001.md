# Handoff — S2-API-001

**Date:** 2026-07-29 23:15 UTC  
**Author/agent:** coding subagent (S2-API-001)  
**Stage:** 2  
**Task packet:** `docs/tasks/active/S2-API-001_api-expansion.md`  
**Status:** COMPLETE  
**Branch/worktree:** `cursor/s2-api-001-expansion-085f`  
**Upstream:** tip with S2-ORCH-001 (`a11c71f` / `9e7ceeb`)  
**Current/final commit(s):** pending push after this handoff

---

## 1. Objective

```text
Additive Stage 2 REST queries/commands under /api/v1/worlds/{world_id}/...
with perspective-safe beliefs, mode-gated Director reads, day-run commands,
and stage2 DeterministicPhaseRunner selection for advance/run-day.
```

## 2. Scope completed

- Stage 2 router `interfaces/http/routes/stage2.py` with:
  - GET day-progress, map, character detail, beliefs, relationships
  - GET NPCs list/detail, diaries/summaries, director hooks/metrics (watcher|director)
  - GET tasks/failures
  - POST run-day, run-until-day, director/propose-event (director mode)
- Shared `interfaces/http/runtime.py` selects `DeterministicPhaseRunner(stage2=True)`
  via fixture/`STAGE2_CHARACTER_IDS`; Stage 1 advance/pause/resume reuse it
- DTOs + OpenAPI regenerate + frontend `schema.d.ts`
- Repo list helpers: locations, NPC list, task failures, active world config,
  get_entity/get_character
- WebSocket envelope docs additive (`day.finalized` / `director.metric` / `day.progress`)
- Integration test `test_stage2_api.py`: day progress, Mira belief isolation from
  Dain endpoint, run-day, director mode gate

## 3. Scope not completed

- Live stream emission of `day.finalized` / `director.metric` (DTO/envelope only;
  ORCH does not yet append those stream_event types)
- NPC archive/retain privileged commands (handbook optional; not required by packet tests)
- Goal/plan edit user-command endpoints (optional handbook item)

## 4. Files changed

| Path | Change |
|---|---|
| `interfaces/http/routes/stage2.py` | created — Stage 2 API |
| `interfaces/http/runtime.py` | created — stage profile runner selection |
| `interfaces/http/dto.py` | Stage 2 DTOs + WS envelope doc model |
| `interfaces/http/routes/stage1.py` | await-safe stage2-selectable runner |
| `interfaces/http/app.py` / `routes/__init__.py` | mount stage2 router |
| `interfaces/http/routes/websocket.py` | additive envelope type notes |
| `application/ports/repositories.py` + SQLAlchemy repos | list/get helpers |
| `backend/tests/integration/test_stage2_api.py` | created |
| `docs/generated/openapi.json` | regenerated |
| `frontend/src/api/generated/schema.d.ts` | regenerated |
| `docs/tasks/active/S2-API-001_api-expansion.md` | COMPLETE |
| `docs/handoffs/2026-07-29_S2-API-001.md` | this handoff |

## 5. Assumptions

```text
ASSUMP-S2-API-001: Stage 2 world detection uses macro_simulation_policy.fixture
  == "stage2", else four focus character IDs present.
ASSUMP-S2-API-002: Beliefs endpoint is owner-scoped only (character_id path);
  no cross-character belief reads.
ASSUMP-S2-API-003: Director GET accepts mode=watcher|director (query or
  X-Observer-Mode); player rejected with 403. Propose-event requires director.
ASSUMP-S2-API-004: run-day / run-until-day force stage2=True runner + fake gateway.
ASSUMP-S2-API-005: Travel summary uses activity rows of type travel (no separate
  travel_progress repository port yet).
```

## 6. Commands and tests run

```bash
uv run ruff check backend scripts tools          # pass
uv run ruff format --check backend scripts tools # pass
uv run basedpyright                              # 0 errors
uv run pytest backend/tests/integration/test_stage2_api.py \
              backend/tests/integration/test_stage1_api.py -q  # pass
uv run python scripts/export_openapi.py          # pass
pnpm --dir frontend generate:api                 # pass
```

## 7. Risks / follow-ups

- Concurrent UI-agent branch churn briefly parked this WIP in stash; verify tip
  on `cursor/s2-api-001-expansion-085f` before merge.
- run-day integration is ~8s with fake models; full seven-day remains scenario suite.
- Director propose-event stores a pending user_command only; ORCH apply path later.

## 8. Next concrete step

Parent merge; unblock S2-UI-001 against OpenAPI paths and S2-QA-001 leakage/API suite.
