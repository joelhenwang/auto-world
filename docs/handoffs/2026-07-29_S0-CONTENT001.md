# Handoff — `S0-CONTENT-001`

**Date:** 2026-07-29  
**Branch:** `cursor/s0-content001-seed-importer-09ce`  
**Status:** ready for review

## Done

- Seed pack `seed/worlds/caldris-embervale-v1/` Stage 0 subset (inn + Mira; Dain authored)
- Deterministic UUIDv5 helpers; load/validate/import
- Atomic import → world/config/clock/location/character/card/state + `WORLD_SEEDED`
- Repeat-import idempotency; broken-ref validation rollback; secret separation
- `scripts/seed_world.py` + Makefile `seed` target
- Repo extensions: location, card, world config/status

## Decisions

1. Event type **`WORLD_SEEDED`** (not `WORLD_INITIALIZED`)
2. `seed_key` map in event `seed_keys_json` + card `secret_manifest.seed_key` (no migration)
3. Phase snapshot deferred to ORCH-002

## Commands

```bash
cd backend && uv run ruff check src tests ../scripts && uv run basedpyright && uv run pytest
```

## Next

Merge with ORCH-001, then S0-ORCH-002 phase runner.
