# Handoff — S2-SIM-001

**Date:** 2026-07-29 22:30 UTC  
**Author/agent:** coding subagent (S2-SIM-001)  
**Stage:** 2  
**Task packet:** `docs/tasks/active/S2-SIM-001_calendar-travel.md`  
**Status:** COMPLETE  
**Branch/worktree:** `cursor/s2-sim-001-travel-085f`  
**Upstream:** Stage 2 tip with S2-DB-001 continuity tables + calendar domain helpers

---

## 1. Objective

```text
Ten-phase calendar helpers / Stage 2 phase profile, deterministic activation
(SLEEP / CONTINUE_ACTIVITY / FULL_DECISION / SKIP), Activity + travel_progress
state machine with seed modifiers, intersecting-route encounters, route
invalidation, and pre-phase model-request estimation.
```

## 2. Scope completed

- Stage 2 phase profile exposing all ten canonical phase names; `walk_full_day`
- Stage 1 activation API preserved; Stage 2 `evaluate_activation_decision`
- Sleep schedule (default night/midnight) + wake/interruption → FULL_DECISION
- Activity SM: start / advance / interrupt / complete / invalidate
- Travel progress uses `distance_units / base_duration_phases` × seed/weather modifier
- Restart-safe: identical inputs reproduce identical progress (no offline catch-up)
- `detect_intersecting_travel` for same/reverse bidirectional routes
- Route invalidation → `INVALIDATED` without fabricating arrival
- `estimate_phase_model_requests` counts only FULL_DECISION (+ optional director/resolver)
- Additive `DeterministicPhaseRunner(stage2=True)` — full calendar, estimate helper;
  mutually exclusive with `stage1`
- Domain: `TravelProgressPersistenceRecord`, `ActivityStatus`, `TravelProgressStatus`
- Unit tests covering full-day sequence, sleep skip, travel-without-LLM, restart,
  intersection, invalidation, request estimate

## 3. Scope not completed

- Full Stage 2 phase execution path (graph fan-out, world-tick travel apply) — ORCH/SIM follow-on
- `travel_progress` repository CRUD (table exists; pure record transforms only here)
- Persistence of sleep/consciousness as separate character columns (passed as params today)

## 4. Files changed

| Path | Change |
|---|---|
| `backend/src/fictional_world/application/simulation/time.py` | created — profiles |
| `backend/src/fictional_world/application/simulation/activation.py` | Stage 2 decisions |
| `backend/src/fictional_world/application/simulation/activity.py` | created — SM/travel |
| `backend/src/fictional_world/application/simulation/request_estimate.py` | created |
| `backend/src/fictional_world/application/simulation/__init__.py` | exports |
| `backend/src/fictional_world/application/orchestration/phase_runner.py` | `stage2` flag |
| `backend/src/fictional_world/domain/continuity/persistence.py` | TravelProgress record |
| `backend/src/fictional_world/domain/continuity/statuses.py` | Activity/Travel statuses |
| `backend/src/fictional_world/domain/continuity/__init__.py` | exports |
| `backend/tests/unit/application/simulation/test_s2_sim_001_calendar_travel.py` | created |
| `backend/tests/unit/application/simulation/test_activation.py` | unchanged (compat) |
| `docs/tasks/active/S2-SIM-001_calendar-travel.md` | COMPLETE |
| `docs/handoffs/2026-07-29_S2-SIM-001.md` | this handoff |

## 5. Assumptions

```text
ASSUMP-S2-SIM-001: Default sleep window = night + midnight; wake = dawn.
ASSUMP-S2-SIM-002: Travel speed modifier ∈ [0.85, 1.15] from world seed hash,
  then × seasonal_modifiers.speed × weather_factor, clamped to [0.5, 1.5].
ASSUMP-S2-SIM-003: Encounter proximity threshold = 0.15 of normalized route progress.
ASSUMP-S2-SIM-004: Route status ≠ "active" invalidates (does not arrive).
ASSUMP-S2-SIM-005: No fictional-time catch-up while stopped; one advance call = one phase.
ASSUMP-S2-SIM-006: stage1 and stage2 runner flags are mutually exclusive.
```

## 6. Commands and tests run

```bash
uv run ruff check …                 # pass
uv run ruff format --check …        # pass (after format)
uv run basedpyright                 # 0 errors
uv run pytest tests/unit -q         # pass (155+)
uv run pytest tests/unit/application/simulation -q  # 26 passed
```

## 7. Risks / unresolved

- Phase runner Stage 2 does not yet apply travel ticks or activation during seal;
  only calendar enablement + `estimate_phase_requests` are wired.
- Activity repository still lacks `update` / travel_progress ports (S2-ORCH / DB follow-on).

## 8. Next concrete step

Wire activation + travel advance into Stage 2 world-tick / snapshot sealing
(S2-ORCH-001 or a small SIM follow-on), then S2-SIM-002 multi-party scenes.
