# Handoff — `S0-ORCH-001`

**Date:** 2026-07-29  
**Branch:** `cursor/s0-orch001-task-outbox-leases-09ce`  
**Status:** ready for review

## Done

- Domain: `OutboxState`, `BudgetStatus`, `RequestBudgetRecord`, claimability helpers
- Ports: `TaskRepository`, `BudgetRepository`; extended `OutboxRepository` + `UnitOfWork`
- Repos: SKIP LOCKED claim for tasks/outbox; budget reserve/consume/release/expire
- Services: `TaskQueueService`, `OutboxDispatcher`, `BudgetService`
- Tests: two-worker claim, expiry reclaim, terminal protection, deps, retry→DL, outbox idempotent ack, budget lifecycle

## Assumptions

- READY→`pending`, LEASED→`claimed`; retry via `available_at`
- Deps require parent `succeeded`
- Budget is data-ops only

## Commands

```bash
cd backend && uv run ruff check src tests && uv run basedpyright && uv run pytest
```

## Next

1. Merge this PR
2. Start S0-CONTENT-001 seed importer
3. S0-ORCH-002 after CONTENT-001 + this merge
