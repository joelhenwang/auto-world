# Handoff — S2-GRAPH-001

**Date:** 2026-07-29 22:45 UTC  
**Author/agent:** coding subagent (S2-GRAPH-001)  
**Stage:** 2  
**Task packet:** `docs/tasks/active/S2-GRAPH-001_stage2-graphs.md`  
**Status:** COMPLETE  
**Branch/worktree:** `cursor/s2-graph-001-graphs-085f`  
**Upstream:** tip with S2-SIM-001 merge (`e255103`) + WORLD/MEM/CHAR/KNOW  
**Current/final commit(s):** `5530dd7`

---

## 1. Objective

```text
Extend decision/reaction/resolver pipelines and add Director/NPC/MemoryConsolidation
graph wrappers — bounded, fake-adapter deterministic, never direct-commit.
```

## 2. Scope completed

- `DecisionGraphInput` additive `goals` / `plans` / `claims` merged into prompt
  variables without breaking Stage 1 sealed-package behaviour
- `ReactionGraphInput.participant_ids` for multi-party awareness (subset checks
  on reactor, attempt actor, and reaction targets)
- Thin plain-async wrappers (no LangGraph required):
  - `DirectorProposalGraph` → `evaluate_director_trigger` + `validate_director_proposal`
    + `safe_no_event_fallback`
  - `MemoryConsolidationGraph` → `consolidate_day` (returns records only)
  - `NPCSceneGraph` → `NpcKnowledgePackage` inputs; model repair path or WAIT fallback
- `restricted_effect_schema` / `restricted_effect_kinds` per `GraphTaskRole`
  (conversation/travel/resolver/director/npc/memory); resolver uses helper
- Malformed JSON repair → one regen → fallback tests (decision + NPC); Stage 1
  graph tests remain green
- Graphs never call `EventCommitService` / never persist

## 3. Scope not completed

- Versioned NPC / Director prompt assets (NPC uses inline stub messages)
- SemanticValidationGraph
- Wiring into seven-day orchestration (S2-ORCH-001)
- Live provider / LangGraph checkpointing

## 4. Files changed

| Path | Change |
|---|---|
| `backend/src/fictional_world/agents/character_decision/graph.py` | additive goals/plans/claims |
| `backend/src/fictional_world/agents/character_reaction/graph.py` | participant_ids multiparty |
| `backend/src/fictional_world/agents/resolver/graph.py` | restricted effect kinds helper |
| `backend/src/fictional_world/agents/restricted_effects.py` | created — role schemas |
| `backend/src/fictional_world/agents/director_proposal/**` | created — thin wrapper |
| `backend/src/fictional_world/agents/memory_consolidation/**` | created — thin wrapper |
| `backend/src/fictional_world/agents/npc_scene/**` | created — knowledge-package stub |
| `backend/src/fictional_world/agents/__init__.py` | export restricted-effect helpers |
| `backend/tests/unit/agents/test_stage2_graphs.py` | created — acceptance tests |
| `docs/tasks/active/S2-GRAPH-001_stage2-graphs.md` | COMPLETE |
| `docs/handoffs/2026-07-29_S2-GRAPH-001.md` | this handoff |

## 5. Assumptions

```text
ASSUMP-S2-GRAPH-001: Empty goals/plans/claims and empty participant_ids preserve
  Stage 1 prompt envelopes exactly.
ASSUMP-S2-GRAPH-001-002: DirectorProposalGraph accepts a pre-parsed proposal;
  model invocation for Director remains ORCH/MODEL follow-on.
ASSUMP-S2-GRAPH-001-003: MemoryConsolidationGraph is async only for orchestration
  symmetry; consolidate_day remains synchronous/pure.
ASSUMP-S2-GRAPH-001-004: NPCSceneGraph uses CHARACTER_DECISION sampling/role for
  stub model calls until a dedicated npc_actor prompt asset exists.
ASSUMP-S2-GRAPH-001-005: Restricted schemas name effect kinds only (JSON fragment),
  not full Pydantic unions — privilege boundary for prompts/validators.
```

## 6. Commands and tests run

```bash
uv run ruff check backend scripts tools          # pass
uv run ruff format --check backend scripts tools # pass
uv run basedpyright                              # 0 errors
uv run pytest backend/tests/unit/agents -q       # Stage 1+2 graph tests pass
uv run pytest -q --tb=line                       # full suite
```

## 7. Risks / follow-ups

- NPC stub prompt is inline (not registry-versioned); promote to `backend/prompts/`
  when ORCH wires batch NPC scenes.
- Resolver still validates Stage 1 envelope structurally; conversation/travel role
  schemas are available for SIM-002 multiparty / travel resolvers to adopt.

## 8. Next concrete step

Wire these graphs into S2-ORCH-001 / S2-SIM-002 multiparty scene assembly; keep
canonical commits on EventCommitService only.
