# Handoff — S3-WORLD-001 / S3-WORLD-002

**Date:** 2026-07-30 00:20 UTC  
**Author/agent:** coding subagent (S3-WORLD-001/002)  
**Stage:** 3  
**Task packets:** `docs/tasks/active/S3-WORLD-001_arcs-factions.md`, `S3-WORLD-002_pacing-novelty.md`  
**Status:** COMPLETE  
**Branch/worktree:** `cursor/s3-mem-rules-world-03fc`  
**Upstream:** S3-DB-001 tip (`ece8f21`)

---

## 1. Objective

```text
Implement Stage 3 arc/hook/faction/settlement pure helpers and soft
novelty/pacing scoring without weakening Stage 2 director/NPC.
```

## 2. Scope completed

### S3-WORLD-001
- Active slot rules: one major arc; up to two active secondary hooks
- Pure helpers: `can_activate_major_arc`, `advance_arc_progress`, `close_arc`,
  `expire_hook` (+ `activate_arc` / `activate_hook` / `close_hook`)
- `apply_faction_daily_update` → indicator/resource deltas + causal promotion
  flag when focus characters are members
- `apply_settlement_indicator_update`
- Plot armour bias default `0` (`DEFAULT_PLOT_ARMOUR_BIAS`)
- Domain records: `NoveltySignaturePersistenceRecord`,
  `SettlementIndicatorPersistenceRecord`

### S3-WORLD-002
- Trope usage + novelty signature builders (domain records; no new migration)
- Soft scoring: `score_director_proposal_with_novelty`
- Metrics: location / participant combo / action family / quiet-dramatic
- Cooldown softens score; `causality_forced` floors score for required recurrence

## 3. Scope not completed

- Trope/novelty SQL repositories on UoW (pure records + scoring only)
- Wiring into phase runner / Director graph
- Effect-command apply path for CREATE_ARC / UPDATE_FACTION_STATE

## 4. Files changed

| Path | Change |
|---|---|
| `backend/src/fictional_world/domain/world/**` | slots, arcs, hooks, factions, settlement |
| `backend/src/fictional_world/application/world/**` | novelty scoring + pacing metrics |
| `backend/src/fictional_world/domain/stage3/persistence.py` | settlement + novelty signature records |
| `backend/tests/unit/domain/world/` | S3-WORLD-001 tests |
| `backend/tests/unit/application/world/` | S3-WORLD-002 tests |

## 5. Assumptions

```text
ASSUMP-S3-WORLD-001: Hook expiry maps to status=abandoned (Stage 2 CK has no expired).
ASSUMP-S3-WORLD-002: Secondary-hook budget uses Stage 2 hook table, not arc_scope=secondary.
ASSUMP-S3-WORLD-003: Soft novelty scoring is additive; Stage 2 validate_director_proposal
  hard trope check is unchanged.
ASSUMP-S3-WORLD-004: Faction daily update never writes character private state;
  promote_causal_event is a flag for Director/resolver only.
ASSUMP-S3-WORLD-005: Trope/novelty repos deferred; scoring uses in-memory NoveltyHistory.
```

## 6. Commands and tests run

```bash
uv run ruff check …                 # pass
uv run ruff format --check …        # pass
uv run basedpyright …               # 0 errors
uv run pytest backend/tests/unit/domain/world \
              backend/tests/unit/application/world -q  # 10 passed
uv run pytest backend/tests/unit/application/director -q  # Stage 2 intact
```

## 7. Next concrete step

Wire arc/faction daily tasks + novelty scoring into S3-ORCH-001 thirty-day
workflow; optionally add thin TropeUsage/NoveltySignature repos on UoW.
