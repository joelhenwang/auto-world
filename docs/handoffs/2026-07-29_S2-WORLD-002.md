# Handoff — S2-WORLD-002

**Date:** 2026-07-29 22:45 UTC  
**Author/agent:** coding subagent (S2-WORLD-002)  
**Stage:** 2  
**Task packet:** `docs/tasks/active/S2-WORLD-002_npc-registry.md`  
**Status:** COMPLETE  
**Branch/worktree:** `cursor/s2-world-002-npc-085f`  
**Upstream:** tip includes S2-WORLD-001 (`dcbfcf3` merge) + KNOW/CHAR/MEM  
**Current/final commit(s):** pending push after this handoff

---

## 1. Objective

```text
Director-only NPC proposal with fingerprint dedup, compact cards, TTL/archive,
budget enforcement, and knowledge packages that never include omniscient
Director data — without promoting NPCs into focus slots.
```

## 2. Scope completed

- `propose_or_register_npc`: fingerprint dedup over name/location/role/traits/hook
  → reuse existing or register new; maps from `ProposedNpcStub`
- Pure budget checks: scene ≤6 detailed; region ≤24 active; ≤3 new named/day
- Compact card builder scrubbing director-only text
- `build_npc_knowledge_package` via KNOW `lookup_perspective_knowledge` +
  `npc_restricted_package_beliefs` + omniscient field strip
- TTL extend on meaningful scene; archive with compact summary; archived cannot
  receive ordinary actor tasks; `recall_archived_npc` for continuity only
- Persistence mappers `to_profile_record` / `to_lifecycle_record` (caller persists)
- Unit tests: duplicate blacksmith; knowledge exclusion; TTL; archive; budgets

## 3. Scope not completed

- Wiring into phase runner / resolver `REGISTER_NPC` / `ARCHIVE_NPC` effects
- Repository `list` / `find_by_fingerprint` / `update_lifecycle` SQL helpers
- NPCSceneGraph / batch actor model calls (S2-GRAPH-001)
- Focus-slot promotion (explicit non-goal)

## 4. Files changed

| Path | Change |
|---|---|
| `backend/src/fictional_world/application/npc/**` | created — registry service |
| `backend/tests/unit/application/npc/test_npc_registry_v1.py` | acceptance unit tests |
| `docs/tasks/active/S2-WORLD-002_npc-registry.md` | COMPLETE |
| `docs/handoffs/2026-07-29_S2-WORLD-002.md` | this handoff |

## 5. Assumptions

```text
ASSUMP-S2-WORLD-002-001: Fingerprint = SHA-256 of normalized
  name|location|sorted roles|sorted traits|hook_key (exact match reuse).
ASSUMP-S2-WORLD-002-002: Default TTL = 20 phases; meaningful-scene extension
  = 10 phases; new TTL = max(current, phase+extension).
ASSUMP-S2-WORLD-002-003: Only lifecycle_status == "active" may receive ordinary
  actor tasks; archived recall does not change status.
ASSUMP-S2-WORLD-002-004: Budget snapshot counters are caller-provided; archived
  NPCs must already be excluded from active/region/scene counts.
ASSUMP-S2-WORLD-002-005: No focus-slot promotion path exists in this package.
ASSUMP-S2-WORLD-002-006: Entity/character row creation remains resolver-owned;
  registry returns validated in-memory entries + persistence record mappers.
```

## 6. Commands and tests run

```bash
uv run ruff check backend/src/fictional_world/application/npc \
  backend/tests/unit/application/npc          # pass
uv run ruff format --check …                  # pass
uv run basedpyright backend/src/fictional_world/application/npc \
  backend/tests/unit/application/npc          # 0 errors
uv run pytest backend/tests/unit/application/npc -q   # 5 passed
uv run pytest backend/tests/unit -q                   # pass
```

## 7. Risks / unresolved

- Dedup is exact-fingerprint only (no fuzzy/trigram yet); near-duplicates with
  different trait sets will register separately.
- No DB list/update helpers yet — orchestration must load existing registry
  entries into memory for `propose_or_register_npc`.

## 8. Next concrete step

Wire registry into Director proposal application / resolver NPC effects when
SIM/ORCH owns that seam; optionally add `NpcRepository.find_by_fingerprint`
and `update_lifecycle` for persistence.
