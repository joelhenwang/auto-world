# Handoff — `S1-MODEL-001`

**Date:** 2026-07-29 18:55 UTC  
**Author/agent:** model subagent  
**Stage:** 1  
**Status:** COMPLETE — awaiting parent review  
**Branch/worktree:** `cursor/s1-model-001-5704` / `/tmp/s1-model`  
**Upstream:** `0c58f6daa111850a1c2a3d814df0b797d311e6fb`

## 1. Completed scope

- Added v1 character-decision, character-reaction, scene-resolver, and scene-narrator
  prompt assets plus bounded repair prompts under `backend/prompts/`.
- Added strict Pydantic metadata, prompt registry, sandboxed Jinja renderer, exact sealed
  variable checks, and SHA-256 source/variable provenance.
- Added a conservative allow-list-aware authored-other-reaction convention check.
- Added all eight requested Stage 1 corpus fixtures.
- Extended `FakeModelGatewayAdapter` with request-ID-first/role-second corpus routing and
  malformed/schema classification while preserving existing script behavior.
- Added prompt/schema/leakage and fake-adapter contract tests.
- Added Jinja2 3.1.6 through `uv add jinja2`.

## 2. Files changed

```text
backend/prompts/**
backend/src/fictional_world/prompts/**
backend/src/fictional_world/infrastructure/model_gateway/fake.py
backend/tests/contract/prompts/**
backend/tests/contract/test_model_gateway_fake.py
backend/tests/fixtures/model_corpus/stage1/**
pyproject.toml
uv.lock
docs/tasks/active/S1-MODEL-001_prompts-schemas-corpus.md
docs/handoffs/2026-07-29_S1-MODEL-001.md
```

Migrations/generated schemas: none. Frozen domain contracts were not modified.

## 3. Verification

| Command | Result |
|---|---|
| baseline `uv run pytest backend/tests/contract/test_model_gateway_fake.py` | 4 passed |
| `uv run pytest backend/tests/contract/prompts backend/tests/contract/test_model_gateway_fake.py` | 17 passed |
| `uv run pytest` | 112 passed, 1 live test deselected |
| `uv run ruff check backend scripts tools` | pass |
| `uv run ruff format --check backend scripts tools` | 185 files clean |
| `uv run basedpyright` | 0 errors, 0 warnings, 0 notes |

No live OpenRouter calls ran.

## 4. Assumptions and risks

- `ActionProposal`, `ReactionProposal`, and `SceneResolution` remain the frozen schema
  implementations; this task adds no duplicate schema definitions.
- Prompt `status` defaults to and is explicitly recorded as `active`, enabling
  `list_active()` while retaining all required metadata fields.
- Narration uses the declared future-facing `SceneNarration@1.0` compatibility label;
  no new domain narration DTO was authorized.
- Authored-other detection is deliberately conservative and accepts allow-listed display
  names from the caller. `S1-GRAPH-001` must retain full domain/semantic validation for
  indirect wording and pronouns; this check is not a general NLP classifier.
- Corpus files are synthetic and default tests remain offline.

## 5. Next action

Parent reviews the diff and reruns the listed acceptance commands, then integrates this
task before `S1-GRAPH-001` and `S1-GRAPH-002`.
