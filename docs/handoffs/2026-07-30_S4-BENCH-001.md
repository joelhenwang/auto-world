# Handoff — S4-BENCH-001

**Date:** 2026-07-30T01:25:00Z  
**Author/agent:** parent coding agent (Stage 4)  
**Stage:** 4  
**Task packet:** `docs/tasks/active/S4-BENCH-001_workload-benchmark.md`  
**Status:** COMPLETE  
**Branch/worktree:** `cursor/s4-integration-8b4a`  
**Upstream commit:** `05db78a`  
**Current/final commit(s):** pending commit on this branch

---

## 1. Objective

Freeze Stage 3 representative JSONL corpus and ship local serving benchmark harness +
selection ADR without hard-coding a production stack before evidence.

## 2. Scope completed

- Stage 4 kickoff status/packets/handoff
- Frozen corpus `stage4-bench-stage3-rep-v1` (18 entries; required roles + case kinds)
- Hardware inventory `docs/ops/hardware/`
- Harness `tools/local_serving_bench/` with dry-run + OpenAI-compatible live adapter
- Candidate runners: vLLM, llama.cpp, Transformers, SGLang, dry-run
- Evidence JSON/MD under `docs/status/evidence/stage-4/benchmarks/`
- ADR-0002 provisional pin: llama.cpp primary; vLLM upgrade gated on live soak
- Unit/contract tests (4) offline green

## 3. Scope not completed

- Live Halo/4060 Ti benchmarks (`local_model_live`) — requires target hardware (ops follow-up)
- S4-MODEL-001+ (next)

## 4. Files changed

See git commit for full list. Key paths: `tools/local_serving_bench/**`,
`backend/tests/fixtures/benchmarks/stage4/**`, `docs/adr/ADR-0002_*`,
`scripts/local_serving/**`, status/task/handoff docs.

## 5. Contracts and interfaces

### Used unchanged

- Stage 0–3 freeze; gateway protocols; domain prompts

### Added or changed

- ADR-0002 serving selection (provisional)
- Benchmark corpus fixture contract

### Deviations

Cloud CI lacks Strix Halo silicon; ADR accepts provisional preference with explicit live
confirmation path rather than inventing GPU soak numbers.

## 6. Database and migrations

```text
Not applicable
```

## 7. Tests and checks run

| Command | Result |
|---|---|
| `uv run pytest backend/tests/unit/benchmark backend/tests/contract/benchmark -q` | 4 passed |
| `uv run basedpyright tools/local_serving_bench` | 0 errors |
| `uv run ruff check/format` on bench paths | pass |
| `./scripts/local_serving/run_dry_run.sh` (+ labelled stacks) | failures=0 |
| Pre-edit offline suite on main | ~318 passed |

## 8. Next concrete step

Implement **S4-MODEL-001** local adapters + capability registry per ADR-0002
OpenAI-compatible interface pin.
