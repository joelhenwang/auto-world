# Handoff — S2-KNOW-001

**Date:** 2026-07-29 22:15 UTC  
**Author/agent:** coding subagent (S2-KNOW-001)  
**Stage:** 2  
**Task packet:** `docs/tasks/active/S2-KNOW-001_claims-beliefs-observation.md`  
**Status:** COMPLETE  
**Branch/worktree:** `cursor/s2-know-001-beliefs-085f`  
**Upstream commit:** `bdcdc70` (task packets) / content+db already on branch tip  
**Current/final commit(s):** pending push after this handoff

---

## 1. Objective

```text
Implement observation→claim→belief pipeline v2 with secret access policy and
leakage tests so four-character weeks never expose unauthorized knowledge.
```

## 2. Scope completed

- Observable fact key masking by visibility requirement + eligibility
- Observer eligibility: direct_witness / hearing_only / partial / absent (no record)
- Observation builder → `ObservationPersistenceRecord` with perceived/omitted facts, confidence, visibility_reason
- Claims: statement/lie → `ClaimPersistenceRecord`; rumour transmission creates NEW claim with provenance linkage
- Beliefs: observation/claim evidence updates confidence clamped to `[0, 1]` with provenance
- `SecretAccessPolicy` — holder sees `secret_key` only via grant rows; injection text ignored
- `lookup_perspective_knowledge` — owner beliefs + held secrets only; strips director-only
- Assembler integration: optional `perspective_knowledge`; Stage 2 fixture beliefs for Iri/Torren; Stage 1 leakage tests unchanged

## 3. Scope not completed

- Wiring observation builder into `EventCommitService` / scene commit (still Stage 1 observer_listed path)
- Persistence of belief_evidence rows via repository (pure record updates only)
- Daily diaries / consolidation (S2-MEM-001)
- CHAR goal services (explicit non-goal)

## 4. Files changed

| Path | Change |
|---|---|
| `backend/src/fictional_world/domain/knowledge/visibility.py` | created — eligibility/intent/truth/access enums |
| `backend/src/fictional_world/domain/knowledge/fact_policy.py` | created — default fact visibility map |
| `backend/src/fictional_world/domain/knowledge/__init__.py` | export persistence + visibility |
| `backend/src/fictional_world/application/knowledge/**` | created — pipeline services |
| `backend/src/fictional_world/application/context/assembler.py` | optional `perspective_knowledge` |
| `backend/src/fictional_world/application/context/fixtures.py` | Stage 2 Iri/Torren beliefs + seed keys |
| `backend/tests/unit/application/knowledge/**` | unit + leakage + injection tests |
| `docs/tasks/active/S2-KNOW-001_*.md` | COMPLETE |
| `docs/handoffs/2026-07-29_S2-KNOW-001.md` | this handoff |

## 5. Assumptions

```text
ASSUMP-S2-KNOW-001: Default fact visibility uses handbook 11 §4.3 keys;
  unknown keys require ACTOR_SEEN (direct witness with LOS).
ASSUMP-S2-KNOW-002: co_located+LOS without close_range → PARTIAL; with
  close_range → DIRECT_WITNESS; precise_close enables amount/substance.
ASSUMP-S2-KNOW-003: Rumour reuses source proposition_key; provenance dict is
  for evidence_summary / audit, not a DB FK column.
ASSUMP-S2-KNOW-004: Assembler still defaults to fixtures when
  perspective_knowledge is omitted (Stage 1 path unchanged).
ASSUMP-S2-KNOW-005: Seed secret_key == proposition_key_for(belief text)
  matching importer slug+hash style.
```

## 6. Commands and tests run

```bash
uv run ruff check backend scripts tools          # pass
uv run ruff format --check backend scripts tools # pass
uv run basedpyright                              # 0 errors
uv run pytest backend/tests/unit/application/context/test_assembler_leakage.py \
  backend/tests/unit -q --tb=line                # pass
uv run pytest -q --tb=line                       # all pass
```

## 7. Assembler integration

- Additive only: new optional kwarg `perspective_knowledge: PerspectiveKnowledge | None`
- When set, replaces fixture private beliefs and may list `held_secret_keys`
- Rejects mismatched observer IDs via `SecretAccessDenied`
- Director-only scrub retained on every sealed section
- Stage 1 `test_assembler_leakage.py` remains green without calling lookup

## 8. Risks / follow-ups

- Commit path still materializes observations without eligibility masking — integrate builder in a later SIM/ORCH task
- Parallel S2-CHAR-001 untracked files were temporarily parked under `/tmp/cursor-park-s2-char-001` during verify so they did not enter this branch commit; restore if that agent still needs them on disk
- Belief confidence update is additive clamp only (no competing-proposition model yet)

## 9. Next concrete step

Parent review/merge; unblock S2-MEM-001 (depends on observations/claims/beliefs) and wire builder into scene/event commit when ORCH owns that seam.
