# Handoff — Stage 4 Kickoff

**Date:** 2026-07-30T01:14:00Z  
**Author:** parent coding agent (Stage 4)  
**From:** Stage 3 GATE_PASS / FROZEN on main `05db78a`  
**To:** Stage 4 implementation (start S4-BENCH-001)  
**Branch:** `cursor/s4-integration-8b4a`  
**Status:** IN_PROGRESS  
**Task packet:** `docs/tasks/active/S4-BENCH-001_workload-benchmark.md`

---

## 1. Confirmed baseline

- Main tip: `05db78a` (merge of Stage 3 PR #37 after #36)
- Alembic head on main: `0005_stage3_long_term_tables`
- Seed: `caldris-embervale-v1` content_version `2`
- Package: `fictional_world` under `backend/src/fictional_world/`
- DB schema name remains `worldsim`
- Stage 3 evidence PASS under `docs/status/evidence/stage-3/`
- Contracts Stages 0–3 FROZEN (`docs/status/CONTRACT_FREEZE.md`)
- Offline suite: default pytest markers deselect live/soak; ~318 passed before edits

## 2. Preflight findings

- Model gateway today: fake + OpenRouter only; no local adapter, no health-aware routing.
- Orchestration: DB task leases with opaque `worker_id`; no host/worker registry or fencing tokens.
- Images / MinIO / ComfyUI: absent from application code; Compose is Postgres-only.
- `ProviderKind` already includes `"local"` in profiles; unused at runtime.
- Live markers `local_model_live` / `image_live` registered but unused.
- This cloud VM is **not** Strix Halo / RTX 4060 Ti hardware. Benchmarks use dry-run /
  recorded fixtures for CI; live hardware confirmation is opt-in via `local_model_live`.

## 3. Stage 4 objective

Topology change only — preserve Stage 3 thirty-day canonical semantics while adding
provider-neutral local routing, durable multi-host jobs, MinIO, ComfyUI images, and
visual continuity. Images never block phases and never become canon automatically.

Primary proof: `stage4-distributed-local-v1` + visual-continuity review under
`docs/status/evidence/stage-4/`.

## 4. Immediate next

Implement **S4-BENCH-001** fully before selecting/hard-coding vLLM, llama.cpp, SGLang,
or Transformers as the production pin. Selection ADR must include rejected alternatives
and rollback.

Do **not** start Stage 5 generation work. Do not weaken Stage 0–3 invariants.
