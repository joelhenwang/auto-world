# Handoff — S2-SIM-002

**Date:** 2026-07-29 22:50 UTC  
**Author/agent:** coding subagent (S2-SIM-002)  
**Stage:** 2  
**Task packet:** `docs/tasks/active/S2-SIM-002_multiparty-scenes.md`  
**Status:** COMPLETE  
**Branch/worktree:** `cursor/s2-sim-002-scenes-085f`  
**Upstream:** tip includes S2-SIM-001 (`e255103` merge)  
**Current/final commit(s):** `e33efa6`

---

## 1. Objective

```text
Multi-party scene assembly for up to four focus + ≤6 detailed NPCs; merge
compatible / same-resource intents via read/write sets; Stage 2 beat budgets;
continuation next phase when budget expires (no in-phase loop); per-NPC
knowledge isolation at assembly metadata.
```

## 2. Scope completed

- Extended Stage 1 `assemble_scenes` (API preserved); added `assemble_multiparty_scenes`
- Focus ≤4 / detailed NPCs ≤6 enforced; co-located / targeted NPCs attach to scenes
- Write-set intersection merges same-resource conflicts; `concurrent_scene_batches`
  / `scenes_are_independent` for parallel-safe partitioning
- Beat budgets per handbook 27 table (+ Stage 1 closing beat on negotiation)
- `evaluate_beat_continuation` / `mark_continuation` — expired budget continues
  next phase; `recursive_loop_allowed` always `False`
- Intra-scene `score_initiative` distinct from global `score_priority`
- `allows_hidden_reaction` always `False`; NPC `knowledge_scope_hash` uniqueness
- Unit tests for all S2-SIM-002 acceptance cases; Stage 1 assembly tests green

## 3. Scope not completed

- Wiring into `DeterministicPhaseRunner` / scene commit continuation_id persistence
- Lethal combat budgets / loops (explicit non-goal)
- Images / narration enqueue

## 4. Files changed

| Path | Change |
|---|---|
| `backend/src/fictional_world/application/simulation/beat_budget.py` | NONLETHAL / NPC_BATCH + exchange helpers |
| `backend/src/fictional_world/application/simulation/conflict_sets.py` | scene write/read sets + concurrent partition |
| `backend/src/fictional_world/application/simulation/priority.py` | `score_initiative` |
| `backend/src/fictional_world/application/simulation/scene_assembly.py` | multiparty assembly + continuation |
| `backend/tests/unit/application/simulation/test_s2_sim_002_multiparty_scenes.py` | acceptance tests |
| `docs/tasks/active/S2-SIM-002_multiparty-scenes.md` | COMPLETE |
| `docs/handoffs/2026-07-29_S2-SIM-002.md` | this handoff |

## 5. Assumptions

```text
ASSUMP-S2-SIM-002-001: Two-person conversation = 2 exchange rounds = 4 beats.
ASSUMP-S2-SIM-002-002: Negotiation keeps Stage 1 closing beat (n*2+1).
ASSUMP-S2-SIM-002-003: ATTACK/DEFEND scenes map to NONLETHAL_CONFLICT (lethal OOS).
ASSUMP-S2-SIM-002-004: Co-located NPCs attach only to social/interactive scenes.
ASSUMP-S2-SIM-002-005: Shared NPC knowledge_scope_hash is rejected at assembly.
ASSUMP-S2-SIM-002-006: SceneDraft domain contract unchanged; AssembledScene wraps it.
```

## 6. Commands and tests run

```bash
uv run ruff check backend/src/fictional_world/application/simulation \
  backend/tests/unit/application/simulation
uv run ruff format --check …   # pass after format
uv run basedpyright backend/src/fictional_world/application/simulation
uv run pytest backend/tests/unit/application/simulation/ -q
uv run pytest backend/tests/unit -q
```

## 7. Risks / unresolved

- Phase runner still calls Stage 1 `assemble_scenes` only — ORCH/SIM follow-on
  should switch to multiparty + persist `continuation_id` when flagged.
- Co-location NPC attachment may over-include background NPCs; tighten with
  explicit invite lists if needed.

## 8. Next concrete step

Wire `assemble_multiparty_scenes` + beat continuation into Stage 2 phase runner /
scene commit (`continuation_id`), then S2-GRAPH-001 reaction graphs for multiparty.
